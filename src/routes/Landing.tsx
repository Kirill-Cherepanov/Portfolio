import {
  FirstSection,
  SecondSection,
  ThirdSection,
  FourthSection,
  PageNavigation,
} from '@/components';

type LandingProps = {};

export function Landing({}: LandingProps) {
  return (
    <div>
      <FirstSection />
      <SecondSection />
    </div>
  );
}
